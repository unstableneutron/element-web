name: Diagnose reusable workflow

on:
    workflow_call:

permissions: {}

jobs:
    diagnose:
        runs-on: ubuntu-24.04
        steps:
            - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
              with:
                  persist-credentials: false

            - run: echo "Local reusable workflows can start"
